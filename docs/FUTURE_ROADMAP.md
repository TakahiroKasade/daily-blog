# 未來路線圖與架構策略 (Future Roadmap & Architecture Strategy)

## 1. 資料庫轉型策略 (Database Strategy)

目前的架構使用 **H2 Database (In-Memory)**，優點是開發快速、零設定，缺點是**重啟後資料會消失**。
若要長期經營並上線，勢必要遷移至持久化資料庫。

### 選項 A：MySQL (經典選擇)
*   **優點**：
    *   老牌、市佔率高，網路上教學資源最多。
    *   許多傳統虛擬主機 (cPanel/WordPress) 都直接支援。
*   **缺點**：
    *   授權較為複雜 (Oracle 擁有)。
    *   在現代雲端平台 (Cloud Native) 的免費層級較少。

### 選項 B：PostgreSQL (現代首選 - 推薦 🏆)
*   **優點**：
    *   **開源社群強大**：功能更新快，支援 JSON、GIS 等進階資料格式。
    *   **雲端友善**：Heroku, Render, Railway, Neon 等新一代雲端平台，提供的免費資料庫幾乎都是 PostgreSQL。
    *   **穩定性高**：在處理高併發 (High Concurrency) 時表現優異。
*   **缺點**：
    *   語法與 MySQL 略有不同 (但透過 JPA/Hibernate 可以無痛切換)。

### 🚀 遷移步驟 (Migration Plan)
1.  **安裝 Docker**：在本地端用 Docker 跑一個 PostgreSQL Container (保持電腦乾淨)。
2.  **修改 `pom.xml`**：加入 `postgresql` 驅動程式。
3.  **修改 `application.properties`**：將 `spring.datasource.url` 指向 Postgres。
4.  **JPA 自動建表**：Spring Boot 會自動幫你在新資料庫建立 Table。

---

## 2. 自動化部署 (CI/CD Automation)

你提到的「不想每次一直做重複的事情」，這就是 **DevOps** 的精髓。
我們可以使用 **GitHub Actions** 來達成「寫完 Code -> 自動上線」的流程。

### 什麼是 CI/CD？
*   **CI (Continuous Integration)**：持續整合。每次 `git push`，GitHub 自動幫你跑編譯 (Build) 和測試 (Test)，確保程式沒壞。
*   **CD (Continuous Deployment)**：持續部署。測試通過後，GitHub 自動把程式打包，推送到伺服器 (如 Render 或 AWS)。

### 推薦流程 (The Golden Path)
1.  **本地開發 (Local)**：使用 **H2**，快速驗證邏輯。
2.  **版本控制 (Git)**：功能完成，執行 `git push origin main`。
3.  **雲端託管 (Hosting)**：使用 **Render.com** (推薦新手)。
    *   它可以直接連結你的 GitHub Repo。
    *   設定：當 `main` 分支有更新，Render 自動拉取程式碼 -> 打包 -> 上線。
    *   **完全免費** (有休眠機制，適合 Side Project)。

### 實作步驟
1.  註冊 [Render.com](https://render.com/)。
2.  點擊 "New Web Service" -> "Build and deploy from a Git repository"。
3.  連結你的 GitHub `daily-blog` repo。
4.  設定 Build Command: `mvn clean package -DskipTests`
5.  設定 Start Command: `java -jar target/blog-0.0.1-SNAPSHOT.jar`
6.  **Done! 以後你只要 `git push`，網站就會自己更新了！**
