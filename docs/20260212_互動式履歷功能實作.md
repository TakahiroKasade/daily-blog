# Phase 4: 互動式履歷 (Interactive Resume) 技術文件

> 日期：2026/02/12
> 開發者：Takahiro Kasade
> 狀態：✅ 完成

---

## 📋 完成項目

| 項目 | 檔案 | 說明 |
|------|------|------|
| ✅ Entity | `Experience.java` | 經歷實體：title, company, description, startDate, endDate, category, skills |
| ✅ Repository | `ExperienceRepository.java` | JPA Repository + `findByCategory()` 自訂查詢 |
| ✅ Service | `ExperienceService.java` | `getAllExperiences()` / `getByCategory()` |
| ✅ Controller | `ResumeController.java` | GET `/resume` → 回傳 resume.html |
| ✅ View | `resume.html` | 時間軸 UI (Timeline) + Navbar 統一 |
| ✅ CSS | `style.css` | Timeline 樣式 (垂直線、圓點、Hover 動畫) |
| ✅ DataSeeder | `DataSeeder.java` | 3 筆測試資料 (WORK / EDUCATION / PROJECT) |
| ✅ DevTools | `pom.xml` + `application.properties` | 熱部署設定 |

---

## 📁 異動檔案

```
新增：
  src/main/java/.../entity/Experience.java
  src/main/java/.../repository/ExperienceRepository.java
  src/main/java/.../service/ExperienceService.java
  src/main/java/.../controller/ResumeController.java
  src/main/resources/templates/resume.html

修改：
  src/main/java/.../config/DataSeeder.java         (新增履歷種子資料)
  src/main/resources/static/css/style.css           (新增 Timeline CSS)
  src/main/resources/templates/index.html            (Navbar 整理 + Resume 入口)
  pom.xml                                            (新增 DevTools 依賴)
  application.properties                             (Thymeleaf cache=false)
  README.md                                          (新增 Phase 4 功能說明)
```

---

## 🎓 Q&A 學習筆記

### Q1: `JpaRepository<Experience, Long>` 泛型的兩個參數分別代表什麼？

```java
public interface ExperienceRepository extends JpaRepository<Experience, Long> {
//                                                          ↑            ↑
//                                                    Entity 型別     PK 型別
}
```

- **第一個** `Experience`：這個 Repository 管理的 Entity 類別
- **第二個** `Long`：該 Entity 的主鍵 (@Id) 資料型別
- JPA 會根據泛型自動產生 `findById(Long)`、`findAll()` 等方法

### Q2: 建立物件時，建構子 vs Setter 有何差異？

```java
// 寫法 A：建構子 — 一行建立，簡潔
Experience e1 = new Experience("Java 工程師", "某公司", "描述", ...);

// 寫法 B：Setter — 逐一設定，可讀性高
Experience e1 = new Experience();
e1.setTitle("Java 工程師");
e1.setCompany("某公司");
```

| | 建構子 | Setter |
|--|--------|--------|
| 優點 | 一行搞定 | 每行看得清楚是哪個欄位 |
| 缺點 | 參數多時容易搞混順序 | 比較冗長 |

### Q3: Spring Boot DevTools 是什麼？為什麼改 HTML 不用重啟？

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-devtools</artifactId>
    <scope>runtime</scope>
</dependency>
```

- **DevTools** 提供熱部署 (Hot Reload) 功能
- 搭配 `spring.thymeleaf.cache=false` 使用
- 改 **HTML/CSS** → 重新整理瀏覽器即可（不用重啟）
- 改 **Java 程式碼** → DevTools 會自動重啟（但還是要等幾秒）

### Q4: Timeline UI 的 CSS 原理是什麼？

```css
/* 垂直線：用 ::before 偽元素畫一條線 */
.timeline::before {
    content: '';
    position: absolute;
    left: 20px;
    width: 2px;
    background: linear-gradient(...);
}

/* 圓點：絕對定位在垂直線上 */
.timeline-dot {
    position: absolute;
    left: 12px;
    border-radius: 50%;         /* 圓形 */
    box-shadow: 0 0 12px ...;   /* 霓虹發光 */
}
```

- `::before` 偽元素產生垂直線（不需要額外 HTML 標籤）
- `.timeline-dot` 用 `position: absolute` 定位在線上
- `.timeline-content` 用 `padding-left` 讓內容避開左側線條

---

## ⏭️ 下一步

- Phase 5: Tech Dashboard (GitHub/Game API 整合)
